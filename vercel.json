{
    "buildCommand": "cd k72 && npm run build",
    "outputDirectory": "k72/dist",
    "installCommand": "cd k72 && npm install",
    "rewrites": [
        { "source": "/(.*)", "destination": "/index.html" }
    ]
}